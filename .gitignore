# ---- Credentials ----
# js/config.js is committed — Supabase anon key is a public key designed for
# client-side use, protected by RLS policies. Airtable PAT is server-side only
# (netlify/functions/airtable-skills.js reads it from process.env).

# ---- OS / Editor ----
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# ---- Netlify local dev ----
.netlify/

# ---- Node (if you ever add tooling) ----
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ---- Env files ----
.env
.env.local
.env.*.local
